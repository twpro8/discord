{
  "name": "lumiere",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "lint": "pnpm --filter frontend lint",
    "generate-client": "pnpm --filter frontend generate-client"
  }
}
